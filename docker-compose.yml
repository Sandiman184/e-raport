# Docker Compose Configuration for E-Raport Production

services:
  # Flask Application
  web:
    build: .
    container_name: raport_app
    restart: unless-stopped
    environment:
      - FLASK_CONFIG=production
      - SECRET_KEY=${SECRET_KEY:-kunci-rahasia-albarokah-2024-production}
      - DATABASE_URL=sqlite:////app/storage/data.sqlite
    volumes:
      - ./storage:/app/storage
    networks:
      - raport_network
    depends_on:
      - nginx

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: raport_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
      - ./storage:/app/storage:ro
    networks:
      - raport_network
    command: "/bin/sh -c 'while :; do sleep 6h & wait $${!}; nginx -s reload; done & nginx -g \"daemon off;\"'"

  # Certbot for SSL
  # This container runs a renewal check loop
  # For manual certificate requests, use: docker compose run --rm --entrypoint certbot certbot certonly ...
  certbot:
    image: certbot/certbot
    container_name: raport_certbot
    restart: unless-stopped
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    # Sleep loop - will be replaced during manual runs
    entrypoint: /bin/sh
    command: -c "while :; do certbot renew --quiet; sleep 12h; done"

networks:
  raport_network:
    driver: bridge

volumes:
  storage:
